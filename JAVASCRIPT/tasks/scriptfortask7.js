`use strict`

// exercise 1
function sub(num1, num2) {
    return num1 - num2;
}

// exercise 2
function welcome (name, age, gender) {
   return `My name is ${name}, I'm ${age} years old and of gender ${gender}`;
}

console.log(welcome(`felix cited`, 20, `male`));

// exercise 3
let powerUp = (n1, n2) => n1**n2